micronaut:
  application:
    name: customers
    instance:
      id: ${random.shortuuid}
  config-client:
    enabled: true
db:
  name: 'customers'
  collection: 'customers'
netty:
  default:
    allocator:
      max-order: 3
endpoints:
  health:
    enabled: true
    sensitive: false